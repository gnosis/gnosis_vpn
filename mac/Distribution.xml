<?xml version="1.0" encoding="utf-8"?>
<installer-gui-script minSpecVersion="2">
    <title>Gnosis VPN</title>
    <allowed-os-versions>
        <os-version min="__MIN_OS_MACOS__"/>
    </allowed-os-versions>
    <organization>com.gnosisvpn</organization>
    <domains enable_anywhere="true"/>

    <!-- Package metadata -->
    <pkg-ref id="com.gnosisvpn.choice.network.rotsee"/>
    <pkg-ref id="com.gnosisvpn.choice.network.jura"/>
    <pkg-ref id="com.gnosisvpn.choice.loglevel.info"/>
    <pkg-ref id="com.gnosisvpn.choice.loglevel.debug"/>
    <!-- The default package needs to be last to run its postinstall as last -->
    <pkg-ref id="com.gnosisvpn.gnosisvpnclient"/>

    <!-- Options -->
    <options customize="allow" require-scripts="false" hostArchitectures="x86_64,arm64"/>

    <!-- JavaScript for radio-style mutual exclusion with opt-out -->
    <script><![CDATA[
        // Last stable selection state of each radio choice.
        // MUST be kept in sync with the start_selected attributes below.
        var radioPrev = {
            'jura': true,
            'rotsee': false,
            'info': false,
            'debug': true
        };

        // Radio semantics for a pair of sibling choices:
        // - clicking one selects it and deselects the sibling (click-to-switch)
        // - unchecking the selected one leaves both unchecked (opt-out)
        // Order-independent: right after a click both siblings may read as
        // selected; the one that was previously selected yields. Each call
        // updates radioPrev only for its own id, so it works whichever
        // sibling Installer re-evaluates first. Requires that both siblings
        // can never be switched on in the same event - hence the group
        // parent choices are enabled="false" (labels, not toggles).
        function radioSelected(myId, siblingId) {
            if (!choices[myId]) {
                return radioPrev[myId];
            }
            var mySel = !!(choices[myId].selected);
            var sibSel = !!(choices[siblingId] && choices[siblingId].selected);
            var result = (mySel && sibSel) ? !radioPrev[myId] : mySel;
            radioPrev[myId] = result;
            return result;
        }
    ]]></script>

    <!-- Installer background -->
    <background file="icon_bg_160x160.png" mime-type="image/png" alignment="bottomleft" scaling="none"/>
    <background-darkAqua file="icon_bg_white_160x160.png" mime-type="image/png" alignment="bottomleft" scaling="none"/>

    <!-- Welcome screen -->
    <welcome file="welcome.html" mime-type="text/html"/>

    <!-- Read Me screen with requirements -->
    <readme file="readme.html" mime-type="text/html"/>

    <!-- Conclusion screen with next steps -->
    <conclusion file="conclusion.html" mime-type="text/html"/>

    <!-- Choices outline - what the user sees -->
    <choices-outline>
        <line choice="network">
            <line choice="jura"/>
            <line choice="rotsee"/>
        </line>
        <line choice="loglevel">
            <line choice="info"/>
            <line choice="debug"/>
        </line>
        <!-- Should be the last choice line to to install previously the choices -->
        <line choice="default"/>
    </choices-outline>

    <!-- Default choice -->
    <choice id="default"
            title="Gnosis VPN Client"
            description="Gnosis VPN client binaries and configuration"
            enabled="false"
            selected="true">
        <pkg-ref id="com.gnosisvpn.gnosisvpnclient"/>
    </choice>

    <!-- Network selection choice (parent group).
         enabled="false": the header is a label, not a toggle - clicking it
         would flip both children on at once, which the radio logic in
         radioSelected() cannot disambiguate. -->
    <choice id="network"
            title="HOPR Network"
            description="Select the HOPR network configuration for your VPN destinations"
            visible="true"
            enabled="false">
    </choice>

    <!-- Rotsee network sub-choice -->
    <choice id="rotsee"
            title="Rotsee"
            description="Connect to VPN nodes on the Rotsee network - US, UK, India"
            visible="true"
            start_selected="false"
            selected="radioSelected('rotsee', 'jura')"
            enabled="true">
        <pkg-ref id="com.gnosisvpn.choice.network.rotsee"/>
    </choice>

    <!-- Jura network sub-choice -->
    <choice id="jura"
            title="Jura (Recommended)"
            description="Connect to VPN nodes on the Jura network - US, UK, NL, Brazil, Australia, India, South Korea"
            visible="true"
            enabled="true"
            start_selected="true"
            selected="radioSelected('jura', 'rotsee')">
        <pkg-ref id="com.gnosisvpn.choice.network.jura"/>
    </choice>

    <!-- Service mode selection choice (parent group).
         enabled="false" for the same reason as the network group header. -->
    <choice id="loglevel"
            title="Service Configuration"
            description="Select the service configuration mode"
            visible="true"
            enabled="false">
    </choice>

    <!-- Standard mode sub-choice -->
    <choice id="info"
            title="Standard Mode"
            description="Install with standard logging configuration"
            visible="true"
            enabled="true"
            start_selected="false"
            selected="radioSelected('info', 'debug')">
        <pkg-ref id="com.gnosisvpn.choice.loglevel.info"/>
    </choice>

    <!-- Debug mode sub-choice -->
    <choice id="debug"
            title="Debug Mode (Recommended for now)"
            description="Install with debug logging enabled - useful for troubleshooting"
            visible="true"
            start_selected="true"
            enabled="true"
            selected="radioSelected('debug', 'info')">
        <pkg-ref id="com.gnosisvpn.choice.loglevel.debug"/>
    </choice>

    <!-- Component package reference -->
    <pkg-ref id="com.gnosisvpn.gnosisvpnclient"
             version="0"
             auth="root"
             installKBytes="16384"
             onConclusion="none">
        #GnosisVPN.pkg
    </pkg-ref>

    <pkg-ref id="com.gnosisvpn.choice.network.rotsee"
             version="0"
             auth="root"
             installKBytes="0"
             onConclusion="none">
        #choice-network-rotsee.pkg
    </pkg-ref>

    <pkg-ref id="com.gnosisvpn.choice.network.jura"
             version="0"
             auth="root"
             installKBytes="0"
             onConclusion="none">
        #choice-network-jura.pkg
    </pkg-ref>

    <pkg-ref id="com.gnosisvpn.choice.loglevel.info"
             version="0"
             auth="root"
             installKBytes="0"
             onConclusion="none">
        #choice-loglevel-info.pkg
    </pkg-ref>

    <pkg-ref id="com.gnosisvpn.choice.loglevel.debug"
             version="0"
             auth="root"
             installKBytes="0"
             onConclusion="none">
        #choice-loglevel-debug.pkg
    </pkg-ref>

    <!-- Localization (default English) -->
    <localization>
        <strings language="en">
            <![CDATA[
                // Installer strings
            ]]>
        </strings>
    </localization>

</installer-gui-script>