# Gnosis VPN: TCP BBR + fq, system-wide. Improves throughput and latency of traffic through the tunnel.
# Settings in /etc/sysctl.conf take precedence over this file.
#
# To disable, delete this file and reset the running values:
#   sudo rm /etc/sysctl.d/99-gnosisvpn-bbr.conf
#   sudo sysctl -w net.ipv4.tcp_congestion_control=cubic
#   sudo sysctl -w net.core.default_qdisc=fq_codel
# Those are the kernel defaults; the install log reports what this host ran before as "(was: ...)".
net.core.default_qdisc = fq
net.ipv4.tcp_congestion_control = bbr
